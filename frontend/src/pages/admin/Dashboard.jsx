// Admin dashboard — same analytics view as superadmin
// Re-exports the superadmin dashboard component (same data, same role permissions on /dashboard/analytics/v2)
export { default } from '../superadmin/Dashboard'
